## Instrucciones 

**Prerequisitos:**  Node.js


1. Instala las dependencias:
   `npm install`
2. Configura la `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Ejecuta la aplicación:
   `npm run dev`